{
  "name": "fabric-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.js && node scripts/optimize-images.js",
    "dev": "vite",
    "build": "npm run sync && node scripts/optimize-images.js && vite build && node scripts/generate.js && node scripts/optimize-images.js --cleanup",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-helmet-async": "^2.0.5",
    "react-router-dom": "^7.1.0",
    "react-transition-group": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.3",
    "vite": "^6.0.11"
  }
}